<!--
文件功能：提供纯自动定时监控、创建告警或持续订阅请求的正式 scope-boundary.md 结构。
职责边界：只记录 out_of_scope 状态、确认未发生的副作用和可选的一次性研究入口；不执行工具预检、数据分析、自动化创建或数据准备。
关联关系：由 ../../SKILL.md 的 out_of_scope 路由物化到 outputs/market-research/<case-id>/02-competitor-intelligence/scope-boundary.md。
-->

# Amazon 竞品情报范围边界说明

## 1. 请求与状态

- Case ID：
- 路由：`out_of_scope`
- 状态：`out_of_scope`
- 用户请求的自动化能力：
- 判定时间：

## 2. 本次明确未创建

- 未创建 cron、计划任务或提醒。
- 未创建价格、排名、Listing 或竞品变化告警。
- 未创建订阅、后台进程、持续追踪器或隐藏状态。
- 未写入自动化配置或凭据。
- 未调用 SIF 或其他业务数据工具。
- 未读取或分析竞品快照。
- 未生成 `data-readiness.md`。

以上项目是已确认的副作用否定清单，不是待执行任务清单。

## 3. 范围边界

- 本 Skill 不拥有调度、告警、通知或持续运行责任。
- 当前只支持用户显式发起的一次当前快照，或一次与既有同口径基线的比较。
- 本文件不表示自动监控已经排队、配置、启用或将在未来执行。

## 4. 可支持的一次性选项

| 选项 | 能回答的问题 | 前置输入 | 用户是否明确接受 | 接受后的路由 |
|---|---|---|---|---|
| 一次当前快照 | 当前竞品集合和 SIF 可见经营结构如何 | Amazon 站点、主/候选 ASIN、研究范围 | yes/no | `snapshot_reverse` |
| 一次基线比较 | 当前快照相对既有同口径基线发生了什么 | Amazon 站点、ASIN、基线、比较字段 | yes/no | `baseline_compare` |

## 5. 转路由闸门

只有用户明确接受上表中的一个一次性选项后，才能结束 `out_of_scope`，重新检查该研究模式的最小输入和工具可用性。用户未接受时保持本状态，不调用工具，也不生成研究报告或数据准备清单。

## 6. 正式结论

- 本次完成的是范围边界说明，不是竞品研究或自动化交付。
- 已创建的唯一正式产物是本 `scope-boundary.md`。
- 若用户接受一次性研究，后续产物将按对应路由单独生成，不能追溯宣称本次已启动自动化。
